{
  "name": "stripe-connect-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Live demo: Stripe Connect OAuth using a generated auth-sdk",
  "scripts": {
    "start": "npx ts-node server.ts",
    "build:sdk": "cd sdk && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.21.0",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "ts-node": "^10.9.0",
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.0"
  }
}
